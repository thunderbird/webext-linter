{
  "_comment": "oldOne is deprecated (deprecated-api, a finding - a deprecated API still exists and still runs, so no construction makes it a non-issue); future was added in TB 200, newer than the declared strict_min_version 128.0, which strict-min-version-api raises for a reader rather than rejecting.",
  "expect": {
    "deprecated-api": [
      "background.js:2"
    ]
  }
}
