{
  "_comment": "Modeled on the mailmindr submission: strict_min_version 102.0 while the compose.onAfterSend event was added in Thunderbird 105, which strict-min-version-api raises for a reader (the golden pins it under Extended Code Review). The pinned strict_max_version on this non-Experiment also trips non-experiment-strict-max-version, which stays a finding. Exercises the version check on an EVENT (not just functions).",
  "expect": {
    "non-experiment-strict-max-version": [
      "manifest.json:8"
    ]
  }
}
