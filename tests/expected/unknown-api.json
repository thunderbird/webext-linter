{
  "_comment": "Unknown namespace (contextMenus) + unknown member (messages.frobnicate). Both are absent from the schema, which is a fact - but whether the add-on is broken by them is not, so unknown-api raises each for a reader instead of rejecting. The golden pins that both reach Extended Code Review with their file:line.",
  "expect": {}
}
