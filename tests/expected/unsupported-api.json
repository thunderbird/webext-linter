{
  "_comment": "runtime.onRestartRequired is annotated version_added: false (a Firefox API documented but unsupported in Thunderbird). unknown-api must still SEE it - this guards the SchemaIndex.isUnsupported(version_added:false) detection from regressing - and raises it for a reader rather than rejecting, so the golden is what pins it.",
  "expect": {}
}
