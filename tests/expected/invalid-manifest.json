{
  "_comment": "Missing version (manifest-missing-key, no location since the key is absent) + bogusPermission (manifest-unknown-permission), both errors; weirdTopLevelKey (unrecognized-manifest-key, info). The present items carry their manifest.json line.",
  "expect": {
    "manifest-missing-key": ["(add-on)"],
    "manifest-unknown-permission": ["manifest.json:4"],
    "unrecognized-manifest-key": ["manifest.json:5"]
  }
}
