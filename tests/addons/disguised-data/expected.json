{
  "_comment": "Exercises the disguised covert-exfil split: the STRONG case (a user-data API call inside a resource URL) is a hard disguised-resource error; the WEAK case (a runtime value appended to a navigation URL, no user-data API) escalates as a disguised-transmission case for a person to judge (see the golden), not a finding.",
  "expect": {
    "disguised-resource": ["background.js:6"]
  }
}
