// The same unauditable VENDOR entry as its XPI twin, in a source code submission.
// The remedy is the same - the declaration names a source the OSV audit cannot use -
// so only the reference the developer is sent to differs.
import "./lib/thing.js";
