// Loaded by the experiment via rootURI.resolve("content/helper.js"). Reachable
// only through that privileged resolver, so the experiment closure keeps it from
// being flagged unused.
globalThis.demoHelper = function demoHelper() {
  return 1;
};
