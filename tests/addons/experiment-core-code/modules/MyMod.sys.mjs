// Loaded by the experiment via importESModule("resource:///modules/MyMod.sys.mjs").
// The resource:// URL is unresolvable, so it is matched by file name; this bundled
// module is reachable, not unused.
export const value = 42;
