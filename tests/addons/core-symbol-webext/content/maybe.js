// Handed to the unsure "mystery" experiment, so its execution context is undetermined
// and it is deferred/exempt - its Services usage is NOT flagged now.
var uri = Services.io.newURI("mailbox://x");
