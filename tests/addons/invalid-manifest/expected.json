{
  "_comment": "Missing version (manifest-missing-key) + bogusPermission (manifest-unknown-permission), both errors; weirdTopLevelKey (unrecognized-manifest-key, warning).",
  "expect": {
    "manifest-missing-key": ["manifest.json"],
    "manifest-unknown-permission": ["manifest.json"],
    "unrecognized-manifest-key": ["manifest.json"]
  }
}
