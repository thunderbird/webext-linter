// Shipped in the XPI but referenced by nothing in the manifest, so the input:xpi
// unused-files check escalates it - an XPI-anchored case a person settles.
console.log("orphan - not reachable from the manifest");
