{
  "_comment": "MV3 schema: browser_action is the wrong-version key. It does not satisfy the action API's manifest-key requirement (missing-manifest-key), and it is an unrecognized MV3 manifest key (unrecognized-manifest-key, info), located at its manifest.json line.",
  "expect": {
    "missing-manifest-key": ["background.js:3"],
    "unrecognized-manifest-key": ["manifest.json:5"]
  }
}
