{
  "_comment": "XPI review exercising the permission scan's site location, including a DOTTED (api-resolved) injection token. messagesRead/accountsRead/compose each escalate with one located site: relatedMessageId (bare), displayedFolder (bare), and messenger.scripting.executeScript (compose's dotted token, resolved via api-usage). scripting is grounded by that call and dropped. Runs with --verbose so the golden keeps the per-site feed detail.",
  "options": {
    "--verbose": true
  },
  "expect": {}
}
